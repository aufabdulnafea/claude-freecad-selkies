services:
  freecad-claude:
    build: .
    container_name: freecad-selkies-claude
    ports:
      - "3000:3000"
    shm_size: '1gb'
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    restart: unless-stopped